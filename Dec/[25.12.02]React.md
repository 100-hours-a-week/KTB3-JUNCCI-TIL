# 📚 React 핵심 개념 정리(Route, Form, Fatch)

---

# <mark> Route </mark>
---


- 웹 애플리케이션에서 **`/home`**, **`/products/1`**, **`/login`** 같은 URL 경로가 있을 때,
    
    해당 경로에 어떤 화면(component)을 보여줘야 할지 결정하는 규칙 또는 설정입니다.
    
- SPA(Single Page Application) 프레임워크(React, Vue, Angular 등)에서 필수적으로 사용됨.

---

SPA 구조에서는 전체 페이지를 새로 불러오지 않고 **하나의 HTML 위에서 화면만 바꿔 렌더링**합니다.

이때 URL과 화면의 상태를 일치시키기 위해 라우팅이 필요합니다.

### Route가 제공하는 기능

- **URL에 따른 화면 전환**
- **브라우저 뒤로가기/앞으로가기 지원**
- **동적 라우팅** (예시: `/product/:id`)
- **코드 스플리팅 및 lazy-loading**
- **URL 파라미터, 쿼리스트링 관리**

---

## Route의 구성 요소

1. **경로(path)**
    - 사용자가 방문할 URL
    - 예: `/`, `/about`, `/users/:id`
2. **컴포넌트(component)**
    - 해당 경로에서 렌더링될 화면
3. **라우터(router)**
    - 여러 Route를 묶어 관리하는 시스템(예: React Router, Vue Router)

---

## 예시로 보는 Route

### React Router 예시

```jsx
import { BrowserRouter, Routes, Route } from "react-router-dom";
import Home from "./Home";
import About from "./About";
import ProductDetail from "./ProductDetail";

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        <Route path="/product/:id" element={<ProductDetail />} />
	         <Route path="*" element={<NotFound />} /> //와일드 카드로 404 에러
	         </Routes>
    </BrowserRouter>
  );
}

```

`/` → Home

`/about` → About

`/product/10` → ProductDetail(10번 상품)

---

## Vue Router 예시

```jsx
import { createRouter, createWebHistory } from "vue-router";
import Home from "./Home.vue";
import About from "./About.vue";

const routes = [
  { path: "/", component: Home },
  { path: "/about", component: About },
];

const router = createRouter({
  history: createWebHistory(),
  routes,
});

export default router;

```

---

## **React Router 핵심 API 정리**

### `navigate("/path")`

컴포넌트 안에서 **자바스크립트 코드로 URL 이동을 수행**하는 함수.

React Router v6에서는 `useNavigate()`로 얻어서 사용한다.

- 버튼 클릭 시 이동
- 로그인 성공 시 자동 이동
- 조건에 따라 페이지 전환할 때
- 비동기 작업(AJAX) 이후 이동할 때

### 예시

```jsx
const navigate = useNavigate();

function goPage() {
  navigate("/about");
}

<button onClick={goPage}>About 이동</button>

```

### 내부 동작

`history.pushState()`를 감싸서 사용 → 새 기록을 쌓으면서 URL 이동.

---

### `<Link to="/path">`

React Router의 **페이지 이동 링크 컴포넌트**

HTML의 `<a>` 태그를 대체하는 역할.

### 사용하는 이유?

- `<a href>`는 페이지를 완전 새로고침함 (SPA 아님)
- `<Link>`는 **pushState를 사용하여 새로고침 없이 화면만 변경**

### 예시

```jsx
<Link to="/about">About 페이지</Link>

```

### 내부 동작

클릭하면 내부적으로 `navigate("/about")`와 거의 동일하게 작동.

즉 `pushState()`를 호출하고 React Router가 UI를 갱신.

---

### `useNavigate()`

`navigate` 함수를 제공하는 React Router의 Hook.

즉, **프로그래밍 방식으로 페이지 이동**하기 위한 Hook.

### 어디에서 사용할 수 있는가?

- 함수형 컴포넌트 내부
- 이벤트 핸들러 / 효과(useEffect) 등에서 사용

### 예시

```jsx
import { useNavigate } from "react-router-dom";

function Login() {
  const navigate = useNavigate();

  function onLoginSuccess() {
    navigate("/dashboard");
  }
}

```

### 특징

- `navigate(-1)` → 뒤로 가기
- `navigate("/home", { replace: true })` → 기록 쌓지 않고 이동

---

### `useLocation()`

현재 URL의 **경로(path) / 검색쿼리(search) / hash / state** 등

**현재 위치 정보를 가져오는 Hook**.

### 언제 사용?

- URL이 바뀔 때 UI를 업데이트해야 할 때
- 현재 경로에 따라 특정 컴포넌트 렌더링
- 쿼리 파라미터 읽을 때
- 페이지를 이동하면서 state를 보낼 때

### 예시

```jsx
import { useLocation } from "react-router-dom";

function MyPage() {
  const location = useLocation();

  console.log(location.pathname);   // "/mypage"
  console.log(location.search);     // "?id=10"
  console.log(location.hash);       // "#section1"
}

```

### 내부 구조

`location` 객체는 다음과 같은 정보를 포함:

```jsx
{
  pathname: "/about",
  search: "?id=3",
  hash: "#top",
  state: { from: "login" },
  key: "abc123"
}

```

---

## **Outlet**

`<Outlet />`은 중첩 라우트(Nested Route)에서

**자식 라우트가 렌더링될 위치**를 지정하는 컴포넌트야.

쉽게 말해,

> “여기 아래에 자식 페이지들이 보여질 거야!”
> 
> 
> 라고 표시하는 자리표(placeholder) 역할.
> 

---

## **왜 Outlet이 필요한가?**

중첩 라우트를 만들 때, 부모 페이지(레이아웃) 안에서

자식 페이지가 바뀌는 구조를 만들고 싶기 때문이야.

예를 들어

- `/dashboard`
- `/dashboard/users`
- `/dashboard/settings`

이렇게 URL에 따라

---

```jsx
<Routes>
  <Route path="/dashboard" element={<DashboardLayout />}>
    <Route path="users" element={<Users />} />
    <Route path="settings" element={<Settings />} />
  </Route>
</Routes>

```

여기서 중요한 점은…

- `/dashboard` → 부모 레이아웃
- `/dashboard/users` → 자식
- `/dashboard/settings` → 자식

---

## **부모 컴포넌트에서 Outlet을 사용**

### DashboardLayout.jsx

```jsx
import { Outlet } from "react-router-dom";

function DashboardLayout() {
  return (
    <div>
      <h1>Dashboard</h1>
      <Outlet />
    </div>
  );
}

export default DashboardLayout;

```

### 출력 흐름

- `/dashboard/users` 접속 → Outlet 위치에 `<Users />` 렌더링
- `/dashboard/settings` → Outlet 위치에 `<Settings />` 렌더링

---

## **Outlet 없는 경우?**

`DashboardLayout` 안에 Outlet이 없다면

자식 페이지가 **어디에도 렌더링되지 않음** → 화면에 안 나와!

그래서 중첩 라우트에는 `<Outlet />`이 필수.

---

## `useParams` — URL 경로 파라미터 읽기

`useParams()`는 **URL 경로에 포함된 동적 파라미터 값을 읽는 Hook**이야.

예:

`/product/10` → `10`

`/user/25/profile` → `25`

---

## 언제 사용하는가?

- URL 경로에 **:id** 처럼 동적 값을 정의했을 때
- 특정 데이터 상세 페이지에서 많이 사용
    
    (게시글 상세, 상품 상세 등)
    

---

## 사용 예시

### ① 라우터 설정

```jsx
<Routes>
  <Route path="/product/:id" element={<ProductDetail />} />
</Routes>

```

### ② 컴포넌트에서 useParams 사용

```jsx
import { useParams } from "react-router-dom";

function ProductDetail() {
  const { id } = useParams();

  return <h1>Product ID: {id}</h1>;
}

```

### 결과

`/product/10` → "Product ID: 10"

---

## 특징

- 반환값은 **객체 형태**
- 모든 파라미터는 문자열(string)
- 경로에 정의된 이름 그대로 읽어야 함

예:

```jsx
<Route path="/user/:userId/:tab" />

```

→ `useParams()` 반환

```jsx
{ userId: "5", tab: "info" }

```

---

## `useSearchParams` — 쿼리스트링(?key=value) 읽고 쓰기

`useSearchParams()`는 **URL의 쿼리 스트링을 읽고 수정할 수 있는 Hook**이야.

예:

`/products?page=2&sort=price`

---

## 언제 사용하는가?

- 검색 필터
- 페이지네이션
- 화면 상태를 URL에 반영할 때
- 새로고침해도 상태가 유지되어야 할 때

---

## 기본 사용 예시

### 쿼리스트링 읽기

```jsx
import { useSearchParams } from "react-router-dom";

function Products() {
  const [searchParams] = useSearchParams();

  const page = searchParams.get("page");
  const sort = searchParams.get("sort");

  return (
    <>
      <h1>Page: {page}</h1>
      <h1>Sort: {sort}</h1>
    </>
  );
}

```

URL:

`/products?page=2&sort=price` → page=2, sort=price

---

## 쿼리스트링 수정하기

`useSearchParams`는 setter를 제공해

URL 쿼리값을 변경할 수 있어.

```jsx
const [searchParams, setSearchParams] = useSearchParams();

function nextPage() {
  const page = parseInt(searchParams.get("page") || 1);
  setSearchParams({ page: page + 1, sort: "price" });
}

```

➡ 페이지는 유지되면서 URL 값만 변경됨

➡ 전체 페이지 새로고침 없이 동작 (pushState 기반)

---

# useSearchParams 특징 정리

### searchParams.get("key")로 값 읽기

값이 없으면 `null`

### setSearchParams()로 값 변경

기존 값을 덮어쓰기 때문에 항상 객체 전체를 넣어야 함.

### 여러 값이 들어갈 수도 있음

예:

```
?filter=tag&filter=active

```

읽을 때:

```jsx
searchParams.getAll("filter")

```

---

# 추가 정리본

<img width="1248" height="600" alt="image" src="https://github.com/user-attachments/assets/f23f6c81-64a0-4351-8d88-0fe8be0282b0" />


이처럼 상태를 전달해 머물렀던 페이지로 로그인 이후 접속가능



---

# <mark> Form </mark>

React에서 Form은 **사용자의 입력을 받아 상태(state)로 관리하는 UI 요소**이다.

일반적인 HTML `<form>`과 같지만, React에서는 입력값과 제출 동작을 **상태(State)와 이벤트 핸들러로 제어**하는 방식이 특징이야.

---

## **Controlled Component 방식**

React에서 가장 많이 쓰는 Form 방식이며 **상태로 input을 완전히 관리**함.

### 특징

- input의 값(value)이 React state와 1:1로 연결됨
- 사용자가 입력할 때마다 state가 업데이트
- 입력값 검증, 조건부 UI 표시가 쉬움

### 예시

```jsx
import { useState } from "react";

function FormExample() {
  const [name, setName] = useState("");

  const handleSubmit = (e) => {
    e.preventDefault(); // 새로고침 방지
    console.log("입력된 이름:", name);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input
        value={name}
        onChange={(e) => setName(e.target.value)}
        placeholder="이름 입력"
      />
      <button type="submit">제출</button>
    </form>
  );
}

```

---

## **Uncontrolled Component 방식**

input 값을 React state로 관리하지 않고

**DOM에서 직접 값을 참조하는 방식**.

`useRef()`를 이용해 값 읽기.

### 특징

- 코드가 간단하고 성능 부담이 낮음
- 단, 입력 상태에 따른 UI 제어가 어려움

### 예시

```jsx
import { useRef } from "react";

function FormExample() {
  const inputRef = useRef();

  const handleSubmit = (e) => {
    e.preventDefault();
    console.log("입력된 이름:", inputRef.current.value);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input ref={inputRef} placeholder="이름 입력" />
      <button type="submit">제출</button>
    </form>
  );
}

```

---

## **Form의 기본 동작**

React의 `<form>`도 기본은 HTML과 동일함:

`type="submit"` 버튼 클릭 → submit 이벤트 발생

`onSubmit` 이벤트로 JS에서 제어

`e.preventDefault()`로 새로고침 방지 (SPA에서 필수)

---

## **Form에서 많이 쓰는 이벤트**

| 이벤트 | 의미 |
| --- | --- |
| **onChange** | 입력값 변경 감지 |
| **onSubmit** | 제출 시 실행 |
| **onFocus / onBlur** | 포커스/포커스 해제 |
| **onInput** | 입력 시 매 타이밍 업데이트 |

---

## **입력 타입별 Form 제어 예시**

### 텍스트 입력

```jsx
<input value={text} onChange={(e) => setText(e.target.value)} />

```

### 체크박스

```jsx
<input type="checkbox"
       checked={checked}
       onChange={(e) => setChecked(e.target.checked)} />

```

### 라디오 버튼

```jsx
<input type="radio"
       name="gender"
       value="male"
       checked={gender === "male"}
       onChange={(e) => setGender(e.target.value)} />

```

### 셀렉트박스

```jsx
<select value={fruit} onChange={(e) => setFruit(e.target.value)}>
  <option value="apple">Apple</option>
  <option value="banana">Banana</option>
</select>

```

---

## 추가 정리

제어 컴포넌트가 적합한 경우

- 검색창, 필터 UI처럼 입력하는 즉시 다른 컴포넌트(상품 리스트)에 영향을 줘야할때, 입력값에 따라서 화면이 바뀌어야할때 (슬라이더 값에 따라서 그래프가 변하는 경우) → 데이터 흐름은 명확

비 제어컴포넌트(+ 폼 라이브러리)가 적합한 경우

- 회원가입, 결제처럼 필드 수가 많고 대부분 단순한 입력일때
- 입력값을 즉시 화면의 다른 부위와 동기화 할 필요는 없고 제출 시점이나 포커스를 잃었을때(blur) 검증하면 충분한 경우
- 저사양 기기를 고려하거나 대규모 폼에서 렌더링 성능 최적화 필요한 경우


# <mark> Fetch </mark>

`fetch()`는 **브라우저에서 제공하는 기본 네트워크 요청 API**로,

서버에 HTTP 요청을 보내고 **Promise 기반**으로 응답을 처리할 수 있는 함수.

쉽게 말하면,

> 서버와 데이터를 주고받는 표준 함수
> 
> 
> (Ajax의 최신 버전 / jQuery의 $.ajax 대체)
> 

---

## **Fetch의 핵심 특징**

### 1) Promise 기반

`async/await`으로 깔끔하게 비동기 처리 가능

### 2) JSON을 쉽게 다룸

응답에서 `.json()` 호출로 바로 JS 객체 변환

### 3) 다양한 요청 설정 가능

- GET
- POST
- PUT
- DELETE
- PATCH
    
    등 거의 모든 HTTP 요청 가능
    

### 4) 브라우저 기본 API

추가 설치 불필요 (Node에서는 `node-fetch` 필요)

---

## **Fetch 기본 사용 예시 (GET 요청)**

```jsx
fetch("https://example.com/api")
  .then(response => response.json())
  .then(data => console.log(data));

```

또는 `async/await`:

```jsx
const response = await fetch("https://example.com/api");
const data = await response.json();
console.log(data);

```

---

## **POST 요청 예시 (JSON 전송)**

```jsx
const response = await fetch("https://example.com/api/users", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    name: "Lee",
    age: 25,
  }),
});

const result = await response.json();
console.log(result);

```

---

## **DELETE 요청 예시**

```jsx
await fetch("https://example.com/api/item/10", {
  method: "DELETE",
});

```

---

## Fetch 응답 처리 방법

## JSON 데이터 가져오기

```jsx
const response = await fetch(url);
const data = await response.json();

```

## text로 받기

```jsx
const text = await response.text();

```

## 상태 코드 확인

```jsx
if (!response.ok) {
  throw new Error("요청 실패!");
}

```

---

## **Fetch 주요 옵션 정리**

| 옵션 | 설명 |
| --- | --- |
| `method` | GET, POST, PUT 등 HTTP 메서드 |
| `headers` | 인증, 콘텐츠 타입 등 헤더 설정 |
| `body` | POST/PUT 요청에 실리는 데이터 |
| `mode` | CORS 설정 |
| `credentials` | 쿠키 포함 여부 |
| `cache` | 캐싱 정책 |

---

## **쿠키 포함해서 요청하기**

```jsx
fetch(url, {
  method: "GET",
  credentials: "include",
});

```

이때 서버도 CORS 설정을 해줘야 정상 동작해.

---

## **Fetch 에러 처리**

fetch는 4xx/5xx 상태 코드를 *에러로 취급하지 않음*

→ 네트워크 자체가 끊겼을 때만 catch 실행됨

따라서 직접 검사해야 함

```jsx
try {
  const res = await fetch(url);

  if (!res.ok) {
    throw new Error("서버 에러");
  }

  const data = await res.json();
} catch (err) {
  console.error(err);
}

```

---

## React에서 Fetch 사용 예시

```jsx
import { useEffect, useState } from "react";

function UserList() {
  const [users, setUsers] = useState([]);

  useEffect(() => {
    fetch("https://example.com/users")
      .then(res => res.json())
      .then(data => setUsers(data));
  }, []);

  return (
    <ul>
      {users.map(u => <li key={u.id}>{u.name}</li>)}
    </ul>
  );
}

```

---
