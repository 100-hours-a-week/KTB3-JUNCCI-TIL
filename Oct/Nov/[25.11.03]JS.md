# 📚 JavaScript 객체와 함수 정리


---

<details>
<summary><strong>1️⃣ 객체 개념 및 생성 방법</strong></summary>


---

### 📍 자바스크립트 객체 사용 방법 3가지
1. **객체 리터럴(Object Literal)**
2. **생성자 함수(Constructor Function)**
3. **클래스(Class)**

---

### 💡 객체란?
- 원시 타입은 하나의 데이터만 표현 가능하고 변경 불가능하다 (Immutable)
- 변경 불가능의 의미: 원시 타입이 존재하고 하나의 데이터만 표현이 가능하고 변경이 불가능 하다.
- 객체는 여러 속성과 메서드를 가질 수 있다
- key와 value로 구성된 속성의 집합이며, value가 함수면 **메서드**

---

### 1. 객체 리터럴

```jsx
const person = {
  name: "Alice",
  age: 25,
  greet: function() {
    console.log(`안녕하세요, 저는 ${this.name}입니다.`);
  },
  run(km) {
    console.log(`뛴다: ${km}km`);
  },
};

person.greet();
```

- 간단하고 빠름  
- 소규모 객체나 일회성 데이터 생성에 적합  

---

### 2. 생성자 함수

```jsx
function Person(name, age) {
  this.name = name;
  this.age = age;
  this.greet = function() {
    console.log(`안녕하세요, 저는 ${this.name}입니다.`);
  };
}

const person1 = new Person("Bob", 30);
person1.greet();
```

- 여러 객체를 반복 생성할 때 유용  
- 반드시 `new` 키워드 사용  

---

### 3. 클래스

```jsx
class Person {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  greet() {
    console.log(`안녕하세요, 저는 ${this.name}입니다.`);
  }
}

const person2 = new Person("Charlie", 28);
person2.greet();
```

- 생성자 함수의 문법적 설탕 (syntactic sugar)
- 상속, prototype 자동 연결  

---

### 요약 비교

| 방법 | 문법 | 사용 용도 | 특징 |
| --- | --- | --- | --- |
| 객체 리터럴 | `{}` | 간단한 객체 | 작성이 쉽고 빠름 |
| 생성자 함수 | `function + new` | 여러 인스턴스 | 전통적 방식 |
| 클래스 | `class + constructor` | 현대적 OOP | 상속 용이 |

</details>

---

<details>
<summary><strong>2️⃣ 객체 동적 추가 / Object.defineProperty()</strong></summary>

### 1. 객체에 동적으로 함수 추가

```jsx
const person = { name: "Alice" };
person.greet = function() {
  console.log(`안녕하세요, 저는 ${this.name}입니다.`);
};
person.greet();
```

- 새로운 속성을 나중에 추가 가능하지만 남발 시 성능 저하 발생

---

### 2. 프로토타입에 함수 추가

```jsx
function Person(name) {
  this.name = name;
}
Person.prototype.greet = function() {
  console.log(`안녕하세요, ${this.name}입니다.`);
};
const p1 = new Person("Bob");
p1.greet();
```

- 모든 인스턴스가 같은 메서드를 참조 (메모리 절약)

---

### 3. 클래스 prototype 수정

```jsx
class Person {
  constructor(name) {
    this.name = name;
  }
}
Person.prototype.greet = function() {
  console.log(`안녕하세요, ${this.name}입니다.`);
};
```

---

### 4. Object.defineProperty()

```jsx
const car = { brand: "Hyundai" };

Object.defineProperty(car, "start", {
  value: function() {
    console.log(`${this.brand} 시동을 겁니다.`);
  },
  writable: true,
  enumerable: true,
  configurable: true
});

car.start();
```

**특징**
- 속성의 `writable`, `enumerable`, `configurable` 제어 가능  
- getter/setter 정의 가능  

```jsx
const user = { _name: "Kim" };
Object.defineProperty(user, "name", {
  get() {
    console.log("이름을 읽습니다");
    return this._name;
  },
  set(value) {
    console.log("이름을 설정합니다:", value);
    this._name = value;
  }
});
```

| 방식 | 예시 | 제어 가능성 |
| --- | --- | --- |
| 일반 방식 | `obj.key = value` | 단순 대입 |
| defineProperty | `Object.defineProperty(obj, "key", {...})` | 정밀 제어 가능 |

</details>

---

<details>
<summary><strong>3️⃣ Getter / Setter / Private 변수</strong></summary>

### Getter & Setter

```jsx
class Person {
  constructor(name, age) {
    this._name = name;
    this._age = age;
  }

  get name() {
    return this._name;
  }

  set name(value) {
    this._name = value;
  }
}
```

- 속성 접근처럼 보이지만 실제로 함수 실행됨  
- 데이터 접근 시 유효성 검사, 로깅 가능  

---

### Private 변수 `#`

```jsx
class Account {
  #balance = 0;
  deposit(amount) { this.#balance += amount; }
  get balance() { return this.#balance; }
}
const acc = new Account();
acc.deposit(1000);
console.log(acc.balance); // 1000
```

- 외부 접근 불가 (캡슐화 완벽)
- ES2020 이상 지원  

---

### Getter + Private 조합

```jsx
class User {
  #password;
  constructor(name, password) {
    this.name = name;
    this.#password = password;
  }
  get password() { return "비밀번호는 볼 수 없습니다."; }
  set password(v) {
    if (v.length < 6) return console.log("6자 이상 필요");
    this.#password = v;
  }
}
```

| 구분 | 선언 방법 | 접근 범위 | 목적 |
| --- | --- | --- | --- |
| Getter | `get name()` | 외부 접근 O | 읽을 때 실행 |
| Setter | `set name(v)` | 외부 접근 O | 설정 시 실행 |
| Private | `#var` | 내부 전용 | 데이터 은닉 |

</details>

---

<details>
<summary><strong>4️⃣ 네임스페이스 & 명명 규칙</strong></summary>

### JS 명명 규칙
- 변수/함수: `camelCase`
- 클래스: `PascalCase`
- 상수: `UPPER_SNAKE_CASE`
- 파일명: `kebab-case`

```jsx
let userName = "민수";
class UserProfile { constructor(name) { this.name = name; } }
const API_KEY = "12345";
```

---

### 네임스페이스란?

> 이름 충돌을 방지하기 위한 논리적 “상자”

```jsx
const MyApp = {};
MyApp.Animal = { type: "Dog" };
MyApp.Person = { name: "민수" };
```

ES6 이후엔 **모듈 시스템**(`import/export`)으로 대체됨.

</details>

---

<details>
<summary><strong>5️⃣ 함수 선언문 / 표현식 / 화살표 함수</strong></summary>

### 함수 선언문

```jsx
function sayHello() { console.log('Hello'); }
sayHello();
```
- 호이스팅 적용됨 (선언 전 호출 가능)

---

### 함수 표현식

```jsx
const sayHello = function() { console.log('Hello'); };
```

- 런타임 시점에 평가  
- 호이스팅 X

---

### 화살표 함수

```jsx
const sayHello = () => console.log('Hello');
```

- 간결한 문법  
- **자체 `this` 없음**, 상위 스코프의 this를 사용  
- `new` 사용 불가  

---

| 구분 | 선언형태 | 호이스팅 | this | 사용 시점 |
| --- | --- | --- | --- | --- |
| 선언문 | `function fn(){}` | 됨 | 자체 this | 선언 전 |
| 표현식 | `const fn = function(){}` | 안 됨 | 자체 this | 선언 후 |
| 화살표 | `()=>{}` | 안 됨 | 상위 스코프 | 선언 후 |

</details>

---

<details>
<summary><strong>6️⃣ 객체 전달, 반환, 전개 연산자</strong></summary>

### 객체 전달 (참조로 전달)

```jsx
function changeUser(user){ user.age = 40; }
const person = {name:"Kim",age:30};
changeUser(person);
console.log(person.age); // 40
```

---

### 복사 (불변성 유지)

```jsx
function changeUser(user){
  const copy = {...user};
  copy.age = 40;
  return copy;
}
```

---

### 전개 연산자 (Spread)

```jsx
const obj = {name:"Kim",age:30};
const newObj = {...obj,city:"Seoul"};
```

---

### Rest 매개변수

```jsx
function sum(...nums){ return nums.reduce((a,b)=>a+b); }
sum(1,2,3);
```

---

### 구조 분해 할당

```jsx
const {name,age} = {name:"Kim",age:30};
const [a,b] = [10,20];
```

</details>

---

<details>
<summary><strong>7️⃣ 구조 분해 할당 (Destructuring)</strong></summary>

배열과 객체에서 필요한 값만 쉽게 추출.

```jsx
const [a,b] = [10,20];
const {name,age} = {name:"Kim",age:30};
```

- 기본값 가능  
- 중첩 구조 가능  
- Rest(`...rest`) 사용 가능  
- 함수 매개변수에서도 사용 가능  

```jsx
function printUser({name,age}) {
  console.log(`${name} is ${age} years old`);
}
```

</details>

---

<details>
<summary><strong>8️⃣ 순수 함수 (Pure Function)</strong></summary>

> 동일 입력 → 동일 출력  
> 외부 상태 변경 X

```jsx
function add(a,b){ return a+b; } // ✅
```

비순수 함수 예시:
```jsx
let count=0;
function increase(){ count++; } // ❌ 외부 변경
```

---

### 대표적 부작용 (Side Effect)
- 전역변수 수정
- console.log
- DOM 조작
- fetch/DB 요청
- Math.random / Date.now

---

### 장점
- 예측 가능성
- 테스트 용이성
- 불변성 유지
- 함수형 프로그래밍 기반

</details>

---

<details>
<summary><strong>9️⃣ 일급 함수 (First-Class Function)</strong></summary>

함수가 값처럼 다뤄지는 언어의 특징.  
➡ 변수에 저장, 인자로 전달, 반환 가능.

---

### 예시

```jsx
const logMessage = msg => console.log(`[LOG]: ${msg}`);
function start() { logMessage('시작'); }
```

고차 함수 (함수를 인자로 받거나 반환):
```jsx
function repeat(n,action){ for(let i=0;i<n;i++) action(i); }
repeat(3,i=>console.log(i));
```

---

### 클로저
함수가 선언될 당시의 스코프를 기억.
```jsx
function makeCounter(){
  let count=0;
  return ()=>++count;
}
const counter=makeCounter();
```

---

### 장점
- 재사용성, 추상화, 캡슐화  
- 함수 조합, 고차 함수, 커리잉 가능  
- 콜백/비동기 패턴의 기반

</details>

---

<details>
<summary><strong>💡 클로저 (Closure)</strong></summary>

> 내부 함수가 외부 함수의 변수에 접근할 수 있는 메커니즘

```jsx
function outer(){
  const name="Kim";
  function inner(){ console.log(name); }
  return inner;
}
const fn=outer();
fn(); // "Kim"
```

---

### 특징
- 외부 변수 기억
- 데이터 은닉 / 캡슐화 가능
- 상태 유지 가능

---

### 예시
```jsx
function createCounter(){
  let count=0;
  return {
    inc:()=>++count,
    get:()=>count
  }
}
```

---

### 주의
- 너무 많은 클로저는 메모리 누수 가능
- 불필요하게 남기지 않기

---

### 장점 요약
| 장점 | 설명 |
| --- | --- |
| 데이터 은닉 | 외부 접근 차단 |
| 상태 유지 | 함수 호출 간 값 기억 |
| 모듈화 | 인터페이스만 노출 |

</details>

---
[JS 객체.pdf](https://github.com/user-attachments/files/23297985/JS.pdf)
[JS 네임스페이스.pdf](https://github.com/user-attachments/files/23298000/JS.pdf)
[JS 함수형 패러다임.pdf](https://github.com/user-attachments/files/23298016/JS.pdf)
[JS 함수형 패러다임 - 순수함수.pdf](https://github.com/user-attachments/files/23298027/JS.-.pdf)
[JS 함수형 패러다임 - 일급 함수.pdf](https://github.com/user-attachments/files/23298052/JS.-.pdf)

